import React from 'react';

function Section() {
  return (
    <section className="section">
      {/* Conteúdo da seção principal (banner, título, etc.) */}
    </section>
  );
}

export default Section;